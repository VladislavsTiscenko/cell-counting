{
  "name": "app-frontend",
  "version": "0.0.0",
  "scripts": {
    "postinstall": "cd frontend && npm install --force",
    "heroku-postbuild": "cd frontend && npm run build"
  },
  "engines" : { 
    "npm" : ">=7.0.0",
    "node" : "==16.6.1"
  }
}
